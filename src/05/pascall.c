#include <stdio.h>
#define N 14

void main(){

    int i, j, k, n=0, a[N][N];
    while(n<=0||n>=13){  /*控制打印的行数不要太大，过大会造成显示不规范*/
        printf("Please set the line number:\n");
        scanf("%d",&n);
    }

    for (i=1;)



}